import { Injectable } from "@nestjs/common";

@Injectable({})
export class Graphservice {
    nextShortestStatus() {
        return 'next shortest status'

    }
    nextShortestSeriesPath() {
        return 'next shortest path'

    }
}
